#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    while (t--) {
        int n, m;
        vector<int> v(n);
        for (int &i : v) cin >> i;
    }
    return 0;
}
